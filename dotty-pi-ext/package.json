{
  "name": "dotty-pi-ext",
  "version": "0.1.0",
  "description": "Pi extension exposing Dotty's voice tools (memory_lookup, think_hard, take_photo, play_song, set_led). Ports bridge.py's _voice_tool_* handlers per #36.",
  "type": "module",
  "private": true,
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "typebox": "^1.1.38"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.74.0"
  },
  "scripts": {
    "test": "npm run test:memory && npm run test:think",
    "test:memory": "node --experimental-strip-types tests/memory_lookup.test.ts",
    "test:think": "node --experimental-strip-types tests/think_hard.test.ts"
  }
}
